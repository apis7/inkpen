{
  "$schema": "../gen/schemas/windows-schema.json",
  "identifier": "default",
  "description": "Minimal surface: window control, native dialogs, and the app's own commands. No filesystem plugin — every disk access goes through a typed Rust command.",
  "//windows": "Additional windows are labelled inkpen-<id>. Without the pattern they inherit no permissions at all: every IPC call fails, the frontend never mounts, and because decorations are off there is no native title bar either — leaving a blank window that cannot even be closed.",
  "windows": ["main", "inkpen-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-title",
    "core:window:allow-set-focus",
    "core:window:allow-show",
    "core:window:allow-maximize",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-inner-size",
    "core:window:allow-outer-position",
    "core:window:allow-scale-factor",
    "core:window:allow-set-always-on-top",
    "core:webview:allow-create-webview-window",
    "core:event:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask"
  ]
}
